- name: Toon directory-inhoud (debug)
  run: |
    pwd
    ls -la
    echo "---- boom (recursief) ----"
    ls -laR | sed -n '1,200p'
- name: Toon directory-inhoud (debug)
  run: |
    pwd
    ls -la
    echo "---- .github ----"
    ls -la .github || true
    echo "---- .github/workflows ----"
    ls -la .github/workflows || true
    echo "---- src ----"
    ls -la src || true
    echo "---- scripts ----"
    ls -la scripts || true
